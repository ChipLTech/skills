interface:
  display_name: "ModelZoo Image Validation"
  short_description: "Qualify local models before DLC and TYD delivery"
  default_prompt: "Use ModelZoo Image Validation for this local model. Treat ModelZoo as optional read-only reference, qualify an ordinary daily base, run C1a, C1b, real-weight functional assertions, and the declared benchmark before image build, then report DLC and TYD as independent targets."
